% Figure properties for 'pitfalls of deconvolution' paper figures

format = 'png'; % for panels tricky for EPS (e.g. Pcolor plots)
color = 'rgb';
dpi = 600;
fontsize = 8;
fontname = 'Arial';
M = 3; % marker size for univariate scatter plots
sym = 'o';  % markers for scatters and strip plots

Units = 'centimeters';

% line widths
widths.plot = 0.75;
widths.error = 0.5;
widths.axis = 0.5;


% panel sizes
figsize.square = [5 5];
figsize.linkedscatter = [3 5];
figsize.dists = [8 4];
figsize.wide = [8 2];
figsize.retina_square = [10 10];
figsize.retina_dists = [16 8];
figsize.retina_wide = [16 4];

% colours for line plots
cmap_gt = varycolor(21);

perf_cmap = brewermap(50,'Blues');
VI_cmap = brewermap(10,'Reds');
% W_cmap = brewermap(10,'PuOr');
G_cmap = [brewermap(4,'Set2'); 0.7 0.7 0.7];

% colours for lins
colors.truth = [0 0 0];
colors.WCM = [0.8 0.4 0.2];
colors.Config = [0.5 0.2 0.1];
colors.poseigs = [0.7 0.7 0.7];

colors.Consensus = [0.8 0.7 0.6];

colors.indicate = [0.7 0.7 0.7];
colors.text = [1 1 1];
colors.retained = [0.3 0.6 0.6];

colors.WCM_map = [0.6 0.3 0; 0.8 0.5 0.2; 1 0.6 0.5];
colors.Config_map = [0.5 0.2 0.1; 0.7 0.4 0.3; 0.9 0.6 0.5];
colors.A_map = brewermap(10,'Greys');

colors.Multiway =  [0.7 0.5 0.8]; % [0.73 0.33 0.82]; % medium orchid [0.8 0.2 0.8];
colors.Multiway_map = [0.4 0.2 0.5; 0.55 0.35 0.65; 0.7 0.5 0.8];
colors.Louvain = [0.3 0.5 0.8];

% plotting ticks and limits for binsize plots

% exportpath
relpath = pwd;
exportpath = [relpath,'/Fig_panels/'];

